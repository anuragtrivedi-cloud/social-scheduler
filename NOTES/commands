npm install => It reads your package.json and installs all required dependencies into node_modules/
It also creates/updates package-lock.json
If you want to install a specific package npm install package-name